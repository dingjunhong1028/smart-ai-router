{
  "name": "@esggo/omni-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ESGGO <-> OmniAgent 全域全端全量雙向同步引擎 (TypeScript, best-practice)",
  "scripts": {
    "typecheck": "tsc -p tsconfig.sync.json",
    "build": "tsc -p tsconfig.sync.json --noEmit false --outDir dist",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.19.43",
    "@types/ws": "^8.5.12",
    "typescript": "^5"
  }
}
